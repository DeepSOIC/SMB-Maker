\Крутые проблемы
(те, что решить быстро не удалось)

после активации пробел нажимает инструмент выделение, а не навигационный режим.

После активации окна ctrl+стрелка делает удвоенный шаг.

Наверно утечка открытых файлов. После сохранения пнг-шки в директорию последнюю невозможно переименовать, пока не закрыта прога.
да нет, вроде всё норм. Надо проверить в XP.

не вдавливаются кнопки, когда работаю пером планшета. Не сразу начинает рисовать планшет.

заставить линзу нормально работать с Aero.

Не пашет прокрутка от тачпада
x Решение: надо реализовать WM_GetObject, для этого нужна MSDN посвежее.
проблема с решением. 1) не помогает, 2) это не суть проблемы. А суть: WM_WHEEL не отлавливается хуком почему-то :(
Исправлено: с помощью запихивания обработчика WM_WHEEL в WndProc MP (сабклассингом). Заодно реализована горизонтальная прокрутка - обработкой WM_HSCROLL.  

формулы из ворда - нерешаемая бяка

какая-то странная проблема с прорисовкой при работе с большими рисунками под вистой без аэро.
причём именно в zoom=1x
сущность: setpixel где в икс-координате установлен бит 8 - не прорисовывается. Баг висты.
х Решение: PSet от VB6 работает, хотя и в три раза тормознутее, но это временное решение.
проблема с решением: откомпилированный pset тоже не работает.
Залечено. Используем Line...BF, причём только в областях где это необходимо.

Если создать сильно наклонный текст большого размера, то там кусок справа обрезается.(у меня: шрифт Vladimir Script, Bold Italic, размерчик 200) 
частично профиксено, всё-равно бяка для многострочного текста. И не работает совсем для шрифта Script italic (ну и хрен с ним собсно)


Проблема копирования-вставки: большое по ширине выделение. Почему-то образуются дополнительные прозрачные области справа-снизу (Vista аэро и без, крупный шрифт (сему вина)).
Та же проблема с открытием жпг'шек. 
GetBitmapDimensionEx(pic.handle,) выдаёт нулевой размер (тестировал на жпг), хотя говорит что ошибки не было :( 
По этой причине ещё происходит:
  пустые области при открытии жпг (прак)
  Неправильно (а может и вообще никак) открывает большие jpg-шки (теор)


Когда отрисовывается скрипт cylinder, периодически вылазит ошибка, из-за кончившегося места для анду.
Частично профиксено - по идее, анду должен стать невозможным, а он остаётся, отменяя только жалкий остаток.

Окно Output: вводить tab убъёшься. 
>>нажатие на кнопку tab не вызывает ни keypress, ни keydown. 

Проблема кисточки. Решить, вероятное решение - по mousemove отрисовывать пока не timeout, а там отрисовывать в таймере. А на маусап - пока не закончится не бросать.
-------------------------------------------------------------------
когда вставляю непрозрачный текст - сохраняется режим выделения, в том числе маска прозрачности. После вставки остаётся некая маска прозрачности, частично обрезающая текст.

Сделать список программ по умолчанию.

aero: если включено растягивание окна - возникают проблемы с мышью

Эффект заменить цвета: сделать опцию "заменить все цвета кроме... на ..."

при Ctrl+V большого рисунка при попытке снять выделение жалуется: entry size exceeds the limit. Please increase the limit. Явно система анду глючит. Надо чтоб спрашивала типа анду не будет, продолжить?
теперь этого случаться не должно! Тестить!

Закончить Import ASCII
1. реализовать функционал
-одна колонка - строка пикселей
-две колонки - 
2. в ресурсы и добавить в список команд

диалог сохранения корячит сетевые пути - приписывает к ним C:\

в функции circle перепутаны rx,ry (в том числе в хелпе) 